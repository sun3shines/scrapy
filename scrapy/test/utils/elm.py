
class Elm(object):

    def __init__(self):
        pass
        # list dict file 等每种数据有自己的存储形式。有自己的存储过程了。是的。
    def parse(self,page):
        pass 

    def push(self):
        pass

    def pop(self):
        pass

# 和数据格式相关，存储文件，比如说http，是要txt的方式。以及link，则是队列，或者数据库了。

# 同理，关于主机对象和elm对象的如何？进行？ 每个主机包括多个elm了。

# 一个框架是多个 例程么？ 是的。调整任务？还是说在一个例程上执行多个主机了？  涉及到任务的优先级了。
